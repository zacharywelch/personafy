<!-- <table class="prose">
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |persona|}}
      <tr>
        <td>{{persona.id}}</td>
        <td>{{link-to persona.name 'personas.edit' persona}}</td>
        <td>{{persona.description}}</td>
      </tr>
    {{/each}}
  </tbody>
</table> -->

<div class='contain clearfix pad2y cards'>
  {{#each model as |persona|}}
    <div class='col-md-4 col-sm-6 pad2'>
      <div class='fill-white row10 round lift'>
        {{link-to persona.name 'persona.edit' persona}}
      </div>
    </div>
  {{else}}
    <p>
      Oops! Create a new persona!
    </p>
  {{/each}}
</div>
